pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                echo 'Source Code Checked Out'
            }
        }

        stage('Build') {
            steps {
                echo 'Building Project'
            }
        }

        stage('Test') {
            steps {
                echo 'Running Tests'
            }
        }
    }
}
